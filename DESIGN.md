# Vigil 관리 콘솔 디자인

사용자가 선택한 A안을 적용한다. 프로젝트·사이트 관리, 자동 기본값과 화면 캡처 중심의 즉시 실행 피드백을 포함한다.

## 적용 범위

`internal/ui`의 업무 개요, 검증 결과, 검사 관리, 시스템 활동, 도움말, 결과 보고서. 기획·QA 담당자를 위한 업무용 웹 콘솔이다. 별도 ProofQA 화면은 이번 개편 대상이 아니다.

## 방향

결재 문서함의 정보 구조를 적용한다. 검토할 일과 그 근거가 중심이고, 서비스 및 실행 설정은 옆에 둔다. 첫 화면은 업무 개요 → 처리할 일 → 최근 검증 순서다. 안내문은 도움말로 이동한다.

## 시각 체계

- 밝은 업무 공간, 짙은 남색 탐색 영역, 파란색 주요 동작.
- Pretendard와 시스템 한국어 글꼴. 본문 15px, 보조 13px, 페이지 제목 28px.
- 표와 구분선으로 상태를 정렬한다. 지표를 장식용 카드로 반복하지 않는다.
- 여백 4/8/12/16/24/32px, 기본 모서리 8px.
- 승인 대기, 오류, 정상, 미검증을 글자와 형태로 구분한다.
- 데스크톱은 고정 탐색 영역과 업무 목록/대상 정보 2열, 모바일은 줄바꿈 가능한 탐색 메뉴와 단일 열.

## 동작과 진실성

탐색, 검색, 상태 필터, 검토/실행 동작과 증거 열람을 보존한다. 조회 모드에서는 접수 버튼을 비활성화하고 이유를 보여 준다. 실행 이력이 없는 상태는 정상이나 모두 확인됨으로 표현하지 않는다. 검사 0건이면 예정 시각을 실제 실행 예정으로 표시하지 않는다.

## 검증

기존 UI/API 테스트, 빌드, 실제 브라우저에서 빈 상태/항목이 있는 상태/연결 실패/요청 불가 상태를 확인한다. 데스크톱·모바일, 밝은·어두운 화면, 페이지 이동과 필터 및 포커스/입력 보존을 검증한다.

## 일반 사용자 흐름

프로젝트 이름과 URL을 입력하면 ID와 호스트를 자동 설정한다. 버그 재현 시나리오나 기능·커밋 설명을 자유롭게 입력하면 에이전트가 E2E 스크립트를 작성하고 검증한다. YAML은 고급 설정으로 접어 둔다. 실행 직후 진행 패널이 열리고 완료 캡처를 바로 보여 준다. 캡처는 확대 대화상자로 열고 상세 기록은 보조 동작으로 제공한다. 상태는 실제 실행기 관찰 훅에서 읽는다.
