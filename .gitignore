# built binary (root only; cmd/vigil/ is source)
/vigil
# local state, evidence, downloaded browsers
.vigil/
/evidence/
bin/
*.db
*.db-wal
*.db-shm
*.log
# machine-specific overrides
vigil.local.yaml
.DS_Store
.playwright-cli/
# domain-specific, local only (ship *.example.* and examples/ instead)
# *.hml: Korean HWPML source documents used as local test material; never shippable
*.hml
/vigil.yaml
/url.md
/scenarios/
/flows/
/features/requests/*
!/features/requests/example.yaml
/.superdesign/dashboard/render/
# legacy names, pending deletion; keep ignored so `git add -A` cannot
# pull the old domain config into a release tree
/qa-flow
/qa-flow.yaml
