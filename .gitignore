# built binaries (root only; cmd/vigil/ is source)
/vigil
/vigil-*
# local state, evidence, downloaded browsers
.vigil/
/evidence/
bin/
*.db
*.db-wal
*.db-shm
*.log
# machine-specific overrides
vigil.local.yaml
.DS_Store
.playwright-cli/
# domain-specific, local only (ship *.example.* and examples/ instead)
# Local working documents and data exports. Releases publish main's tree verbatim with
# no filter, so anything dropped in the working copy ships unless it is named here.
# The .hml files were the first case; student result exports were the second.
*.hml
*.hwp
*.hwpx
*.xlsx
*.xls
*.csv
*.docx
*.doc
*.pptx
*.ppt
/vigil.yaml
/url.md
/scenarios/
/flows/
/features/requests/*
!/features/requests/example.yaml
/.superdesign/dashboard/render/
# legacy names, pending deletion; keep ignored so `git add -A` cannot
# pull the old domain config into a release tree
/qa-flow
/qa-flow.yaml

# domain packs are local (ship examples/ instead)
/packs/
/tasks/hitl-qa/loki-sample.json

# Local visual design exploration and QA captures
.impeccable/
